% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report-data.R
\name{connectivity_report_data}
\alias{connectivity_report_data}
\title{Everything one connectivity analysis produces}
\usage{
connectivity_report_data(
  habitat,
  barrier,
  species,
  interpatch_distance = NULL,
  buffer_radius = NULL,
  verbose = TRUE
)
}
\arguments{
\item{habitat}{Terra SpatRaster. The habitat layer.}

\item{barrier}{Terra SpatRaster. The barrier layer.}

\item{species}{Species name. E.g., "Superb Fairy Wren".}

\item{interpatch_distance}{Numeric. The distance (in metres) at which
habitat patches are considered connected. May be a scalar or a vector.
Provide exactly one of \code{interpatch_distance} or \code{buffer_radius}.}

\item{buffer_radius}{Numeric. The radius in metres around the habitat, an
alternative to \code{interpatch_distance}.}

\item{verbose}{Logical. Display progress messages (default: TRUE).}
}
\value{
A \code{connectivity_report_data} object: a list of
\itemize{
\item \code{connectivity}, a \code{connectivity} summary with one row per distance
\item \code{habitat} and \code{barrier}, the layers as supplied
\item \code{buffered_habitat} and \code{patch_id_raster}, lists of \code{SpatRaster}, one per
distance and named by it
\item \code{species} and \code{interpatch_distance}
}
}
\description{
Runs the connectivity pipeline once per distance and keeps both the summary
and the spatial layers the maps need. \code{\link[=habitat_connectivity]{habitat_connectivity()}} returns the
summary alone; this also keeps the buffered habitat and the patch-ID
raster for each distance, which is what the downloadable assets and the
report are built from.
}
\examples{
\donttest{
report_data <- connectivity_report_data(
  habitat = example_wren_habitat(),
  barrier = example_wren_barrier(),
  species = "Superb Fairy Wren",
  interpatch_distance = 200,
  verbose = FALSE
)

report_data$connectivity
names(report_data$patch_id_raster)
}
}
\seealso{
\code{\link[=habitat_connectivity]{habitat_connectivity()}} when only the summary is wanted.
}
