% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/habitat-connectivity-comparison.R
\name{habitat_connectivity_comparison}
\alias{habitat_connectivity_comparison}
\title{Compare habitat connectivity of a scenario against a baseline}
\usage{
habitat_connectivity_comparison(
  habitat_scenario,
  barrier_scenario,
  habitat_baseline,
  barrier_baseline,
  species,
  interpatch_distance = NULL,
  buffer_radius = NULL,
  verbose = TRUE
)
}
\arguments{
\item{habitat_scenario}{Terra SpatRaster. The scenario habitat layer.}

\item{barrier_scenario}{Terra SpatRaster. The scenario barrier layer.}

\item{habitat_baseline}{Terra SpatRaster. The baseline habitat layer.}

\item{barrier_baseline}{Terra SpatRaster. The baseline barrier layer.}

\item{species}{Species name. E.g., "Superb Fairy Wren".}

\item{interpatch_distance}{Numeric. The distance (in metres) at which habitat
patches are considered connected. May be a scalar or a vector; a vector
runs the comparison once per distance. Provide exactly one of
\code{interpatch_distance} or \code{buffer_radius}. See \code{\link[=habitat_connectivity]{habitat_connectivity()}} for
the interpatch distance / buffer radius relationship.}

\item{buffer_radius}{Numeric. The radius in metres around the habitat, an
alternative to \code{interpatch_distance}. Provide exactly one of
\code{interpatch_distance} or \code{buffer_radius}. See \code{\link[=habitat_connectivity]{habitat_connectivity()}}.}

\item{verbose}{Logical. Display progress messages (default: TRUE).}
}
\value{
A \code{compare_connectivity} object: a tibble with four rows
(\code{baseline}, \code{scenario}, \code{change}, \code{pct_change}) per interpatch distance. A
scalar \code{interpatch_distance} gives four rows; a length-3 vector gives
twelve rows (four per distance). See \code{\link[=compare_connectivity]{compare_connectivity()}} for the
columns.
}
\description{
A spatial-in wrapper around \code{\link[=compare_connectivity]{compare_connectivity()}}. Give it a scenario
habitat/barrier pair and a baseline habitat/barrier pair and it runs the
full connectivity pipeline on each (via \code{\link[=habitat_connectivity]{habitat_connectivity()}}) and
compares them. Provide either an \code{interpatch_distance} or a \code{buffer_radius}
(as with \code{\link[=habitat_connectivity]{habitat_connectivity()}}); \code{interpatch_distance} may be a vector,
in which case the comparison is run once per distance and the per-distance
results are stacked.
}
\details{
Change exactly one layer between baseline and scenario so the difference is
attributable to a single variable. To assess a barrier change, pass the
same habitat to both \code{habitat_baseline} and \code{habitat_scenario}; to assess a
habitat change, pass the same barrier to both \code{barrier_baseline} and
\code{barrier_scenario}. If both the habitat \emph{and} the barrier differ the
function aborts; if neither differs it warns (the scenario equals the
baseline, so every \code{change} value is zero).

Note that this runs the full connectivity pipeline \code{2 * length(interpatch_distance)} times (buffering is the slow step), so a long
vector of distances will take proportionally longer.
}
\examples{
\donttest{
habitat_connectivity_comparison(
  habitat_scenario = example_wren_habitat(),
  barrier_scenario = example_wren_barrier_scenario(),
  habitat_baseline = example_wren_habitat(),
  barrier_baseline = example_wren_barrier(),
  species = "Superb Fairy Wren",
  interpatch_distance = 200,
  verbose = FALSE
)
}
}
\seealso{
\code{\link[=compare_connectivity]{compare_connectivity()}}, \code{\link[=habitat_connectivity]{habitat_connectivity()}}
}
