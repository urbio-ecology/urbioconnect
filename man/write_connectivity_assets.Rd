% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assets.R
\name{write_connectivity_assets}
\alias{write_connectivity_assets}
\title{Write the downloadable assets for one analysis}
\usage{
write_connectivity_assets(x, dir)
}
\arguments{
\item{x}{A \code{connectivity_report_data} object from
\code{\link[=connectivity_report_data]{connectivity_report_data()}}.}

\item{dir}{Directory to write into. Created if it doesn't exist.}
}
\value{
The manifest, invisibly: a tibble of \code{path}, \code{kind} and
\code{description}, one row per file written, with paths relative to \code{dir}.
}
\description{
Writes every map, table and GIS layer a planner needs into \code{dir}, laid out
in folders by interpatch distance, with a README explaining each file.
\code{\link[=zip_connectivity_assets]{zip_connectivity_assets()}} wraps this into a single archive, which is
what the Shiny app's download button uses.
}
\examples{
\donttest{
report_data <- connectivity_report_data(
  habitat = example_wren_habitat(),
  barrier = example_wren_barrier(),
  species = "Superb Fairy Wren",
  interpatch_distance = 200,
  verbose = FALSE
)

assets <- write_connectivity_assets(report_data, dir = tempfile())
assets$path
}
}
\seealso{
\code{\link[=connectivity_report_data]{connectivity_report_data()}}, \code{\link[=zip_connectivity_assets]{zip_connectivity_assets()}}
}
